// Exercice 7: Implémenter une fonction de réduction personnalisée
const array1 = [1, 2, 3, 4,20];
const ar = array1.reduce(
    (accumulateur, valeurCourante) => accumulateur + valeurCourante
  );
  console.log(ar);